
git clone https://github.com/woorisx/fs.git


깃이그노어 만들기
https://www.toptal.com/developers/gitignore/
=======================

git --version

git config --global user.name "woorisx"
git config --global user.email "suhwooree@gmail.com"
//git 개인인증정보는 window에 내부에 저장됨
git clone https://github.com/woorisx/fs.git

Authentication 깃허브 비밀번호 재입력하면
Authentication Succeeded
You may now close this tab and return to the application.

==업로드==
git add .
git commit -m "테스트"
git push -u origin main

==다운로드==
서버에서 다운로드는 풀
git pull origin main

==로그인==
## 2. GitHub 로그인

내 컴퓨터의 터미널(CLI)과 깃허브 계정을 안전하게 연결(인증)

bash
gh auth login

? Where do you use GitHub? GitHub.com
? What is your preferred protocol for Git operations on this host? HTTPS
? Authenticate Git with your GitHub credentials? Yes
? How would you like to authenticate GitHub CLI? Login with a web browser

! First copy your one-time code: 1503-BE6A
Press Enter to open https://github.com/login/device in your browser...
✓ Authentication complete.
- gh config set -h github.com git_protocol https
✓ Configured git protocol
✓ Logged in as fsclass-n
PS D:\git\py2026>


git bash (원하는 디렉토리 우클릭후 실행)

dir

mkdir gittest

cd gittest

git init

git branch -m master main

> gh repo create gittest -public --source=. --push(처음엔 안씀, 올릴파일이 없기때문)
* repo : repository

gh repo create gittest --public --source=.




==cmd==

디렉토리생성 후
디렉토리> gh auth refresh -h github.com -s delete_repo
gh repo delete woorisx/gittest --yes



==깃허브데스크탑==

삭제는 서버에서
생성은 로컬에서 먼저

==vscode==
마우스 우클릭 git bash here
> code .



==초기화==
git init

==연결==
git remote add origin <url>

==데이터 가져오기==연결만 됐지 다운로드 받지 못함
git fetch origin

==브랜치확인하기==
git branch
git branch -a(전체 리포지토리 보여줌)

==브랜치로 이동==
git checkout <branch> 
git checkout -b <brach>새로 생성

==새로생성==
git checkout -b feature/woori 

==다운로드==
git push -u origin feature/woori 



--------------------- 깃 버전관리
------------ 커밋 이전 상태
git status
------------ 커밋 이후 상태
git log
---------hashcode가 일곱자이상 일치하면 됨
git log --oneline

------------ 위의 명령어보다 상세하게 파일 안의 내용까지 확인
git log -p

------------ 해당 폴더에 파일생성
echo "Hello World" > app3.txt

------------ 생성된 파일 내용 확인
cat app3.txt

------------ 스테이징된 app3.txt를 트랙할수없는 파일로 내림
git restore --staged app3.txt

------------ 파일 수정

------------ 커밋 되돌리기
------- -soft(커밋만되돌림), -mixed, -hard(못돌아옴)

git revert 
/git reset --soft <해시코드> 
